import { CommunityCharactersClient } from "./community-client";

export const metadata = {
  title: "Community Characters — Artifacial",
};

export default function CommunityCharactersPage() {
  return <CommunityCharactersClient />;
}
